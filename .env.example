# Server
PORT=4173
HOST=0.0.0.0

# Runtime data directory (mapped to persistent volume on Unraid)
DATA_DIR=/data

# Optional: override container user/group (run container with --user ${PUID}:${PGID})
PUID=1000
PGID=1000
